# SIL PoC - Environment Variables
# Classificador Multimodal de Gestos

# Python/PyTorch Settings
PYTHONPATH=.
PYTORCH_ENABLE_MPS_FALLBACK=1

# Training Configuration (Optimized for >90% accuracy)
POC_SEED=42
POC_EPOCHS=50
POC_SAMPLES_PER_EPOCH=200
POC_LEARNING_RATE=0.02
POC_MOMENTUM=0.9
POC_NOISE_LEVEL=0.10
POC_LR_STEP_SIZE=20
POC_LR_GAMMA=0.5
POC_EARLY_STOPPING_PATIENCE=5

# Benchmark Configuration
POC_BENCHMARK_ITERATIONS=1000
POC_WARMUP_ITERATIONS=100

# SIL Model Configuration
SIL_TARGET_MODE=SIL-128
