# SIL VSP Interpreter Configuration
# Copy this file to .env and adjust values as needed

# Carbon Intensity (gCO2e per kWh)
CARBON_INTENSITY_BRAZIL=0.075        # Brazil grid average (hydroelectric-heavy)
CARBON_INTENSITY_WORLD=0.475         # Global grid average

# CPU Energy Model Parameters
CPU_BASELINE_POWER_W=15.0            # Baseline CPU power consumption (Watts)
CPU_FREQUENCY_GHZ=3.0                # CPU frequency (GHz)

# Performance Budgets
MAX_ENERGY_BUDGET_J=1.0              # Maximum energy budget per inference (Joules)
LATENCY_BUDGET_US=100000             # Maximum latency budget (microseconds, 100ms)
